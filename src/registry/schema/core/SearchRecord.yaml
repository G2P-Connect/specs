type: object
description: |
  Search record based on filter attribute values i.e, pre defined record types or list of custom key value attributed
properties: 
  record_type:
    oneOf: 
      - $ref: RecordType.yaml
      - $ref: ../civil/RecordType.yaml
      - $ref: ../../../extensions/aadhaar/RecordType.yaml
      - $ref: ../../../extensions/dci/RecordType.yaml
  data:
    type: object
    description: |
      Holds record object as defined by above record_type by respective sender/receiver systems