type: object
description: |
  Search result record as an outcome of search/subscribe action
properties: 
  record_type:
    $ref: AllFilters.yaml
  data:
    type: object
    description: |
      1. Holds record object as defined by above record_type by respective sender/receiver systems
      2. Objects are defined in respecitive schemas definition with entity/org namespace as prefix
      3. Implementing systems shall reference to the requires objects based on the context of these API usage