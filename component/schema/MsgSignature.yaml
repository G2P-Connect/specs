type: string
description: "Signature of message body verified using sender's signing public key. <br/><br/>
        1. <code>Signature : keyId=\"{sender_id}|{unique_key_id}|{algorithm}\",algorithm=\"ed25519\",created=\"1606970629\",expires=\"1607030629\",headers=\"(created) (expires) digest\",signature=\"Base64(signing string)\"</code>"