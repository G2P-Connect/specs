type: object
description: 'Payments status list as a collection for bulk processing'
properties:
  header:
    $ref: "./PaymentBatchStatusHeader.yaml"
  payments_status:
    type: array
    items:
      $ref: './PaymentStatus.yaml'
required:
  - header
  - payments_status