type: object
description: Search subscriptions based on registry type and event type
properties:
  transaction_id: 
    $ref: ../../../common/schema/TransactionId.yaml
  reference_id:
    $ref: "../../../common/schema/ReferenceId.yaml"
  timestamp: 
    $ref: "../../../common/schema/DateTime.yaml"
  registry_type:
    $ref: "./RegistryType.yaml"
  event_type:
    $ref: AllEvents.yaml
  locale:
    $ref: "../../../common/schema/LanguageCode.yaml"   
required:
  - transaction_id 