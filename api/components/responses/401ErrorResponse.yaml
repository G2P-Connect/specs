description: "Authorization errors due to invalid authorization / access token<br>
  <b>code : message</b> <br>
  401-1000 : tbd <br>
  401-1001: tbd <br>"
headers:
  x-request-id:
    $ref: '../headers/x-request-id.yaml'
  x-correlation-id:
    $ref: '../headers/x-correlation-id.yaml'
  date:
    $ref: "../headers/date.yaml"
  x-processing-time:
    $ref: "../headers/x-processing-time.yaml"
content:
  application/json:
    schema:
      $ref: "../schemas/common/ErrorResponse.yaml"