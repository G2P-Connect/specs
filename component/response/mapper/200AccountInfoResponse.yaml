description: "TODO: <br>
  1. Should payment_address be included? <br> 
  2. Should any additiona information be removed from the response?"
headers:
  x-request-id:
    $ref: '../../headers/x-request-id.yaml'
  x-correlation-id:
    $ref: '../../headers/x-correlation-id.yaml'
  date:
    $ref: "../../headers/date.yaml"
  x-processing-time:
    $ref: "../../headers/x-processing-time.yaml"
content:
  application/json:
    schema:
      $ref: "../../schemas/common/AccountInfo.yaml"