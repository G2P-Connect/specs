description: Person demographics verify object
allOf:
- properties:
    name:
      description: Person full name
      type: string
    dob:
      description: date of brith
      type: string
      format: date
    gender:
      description: gender
      type: string
      enum:
        - "male"
        - "female"
        - "transgender"
- $ref: "../../../common/schema/BeneficiaryContact.yaml"
- properties:
    attributes:
      $ref: "../../../common/schema/ExpressionList.yaml"
