type: string
description: |
  The SP-System receives data from Disability registry, usually the DR sends registration and updates events to SP-System

  1 : Register 
enum:
  - "REGISTER"
example: "spdci-common:RegistryEventType:REGISTER"